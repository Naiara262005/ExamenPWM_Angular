// TODO: [EXAMEN] Cambiar el nombre de la interfaz por la real (Ej: Grade, Task)
export interface BaseRelacional {
  id?: string; // El ID de Firebase, lo dejamos opcional con el '?'
  
  // TODO: [EXAMEN] Añadir aquí debajo los campos que pida el enunciado.
  // Ejemplos:
  // name: string;
  // email: string;
  // description: string;
  //studentId: string; // Añadimos esto
  //subjectId: string; // Añadimos esto
  //grade: number;
}